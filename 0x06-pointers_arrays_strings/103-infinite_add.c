#include "main.h"

/**
 * infinite_add - Adds two numbers
 * @n1: first number
 * @n2: second number
 * @r: result buffer
 * @size_r: buffer size
 *
 * Description: Assuming that it will always get positive numbers or 0,
 * only digits in the strings, the string will never be empty.
 *
 * Return: Pointer to result, 0 if result can not be stored in @r.
 */
char *infinite_add(char *n1, char *n2, char *r, int size_r)
{

}
